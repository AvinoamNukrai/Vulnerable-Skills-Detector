# Open Questions for the TA

1. Does top-N mean top repositories or top individual skills?
2. Should v1 include only strict `SKILL.md` skills?
3. Should nonstandard Claude commands be collected separately?
4. Is GitHub sufficient for the first submission?
5. Should forks be excluded by default?
6. Should archived repositories be excluded?
7. Should the exported dataset contain full repositories or only skill directories?
8. Are auxiliary files outside the skill directory ever required?
9. Is there a target number of valid skills, beyond top 50 repositories?
10. Should the final downloaded corpus be submitted, or only manifests and scripts?
11. Should we preserve exact repository licenses in the manifest?
12. Is a manually inspected validation sample expected?
13. Should the crawler be optimized for one-time collection or incremental refreshes?
14. Should Part 2 scanners receive strict-only skills, lenient skills, or both?
15. Is there an expected report format for the crawler/dataset component?
